<% provide(:title, @user.name) %>
<%= render 'users/user_info' %>
<div class="background">
  <%= will_paginate @plans %>
  <ol class="plans">
    <%= render @plans, user: @user %>
  </ol>
  <%= will_paginate @plans %>
</div>
