
  <% if key.to_s == plan_detail.action_type %>
    <label class="btn btn-default active">
      <%= plan_detail_f.radio_button :action_type,
                                     key,
                                     {checked: true, class: 'radio-btn'} %>
      <%= value %>
    </label>
  <% else %>
    <label class="btn btn-default">
      <%= plan_detail_f.radio_button :action_type,
                                     key,
                                     {checked: false, class: 'radio-btn'} %>
      <%= value %>
    </label>
  <% end %>
